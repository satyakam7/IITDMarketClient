const initState = {
    userStack: {},
    userPStack: {},
    chats: [],
    rooms: [],
    socket: null,
    error: null,
};

const chatReducer = (state = initState) => {
    return state;
};

export default chatReducer;
