export interface Item {
    id: number;
    name: string;
    price: string;
    category: string;
    image: string;
    date: Date;
    condition: string;
    description: string;
}

export interface User {
    name: string;
}
